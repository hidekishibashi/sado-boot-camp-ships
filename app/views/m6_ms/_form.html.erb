<%= form_with(model: m6_m) do |form| %>
  <% if m6_m.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m6_m.errors.count, "error") %> prohibited this m6_m from being saved:</h2>

      <ul>
        <% m6_m.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.number_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
