<%= form_with(model: mybro) do |form| %>
  <% if mybro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mybro.errors.count, "error") %> prohibited this mybro from being saved:</h2>

      <ul>
        <% mybro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :likes %>
    <%= form.text_field :likes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
